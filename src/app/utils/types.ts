export type Employee = {
    city: string,
    department: string,
    departmentId: string,
    email: string,
    fullName: string,
    gender: string,
    hireDate: string, // UTC format
    id: string,
    isPermanent: Boolean,
    mobile: string, // UK format
}
